name: Build Kernel S9 Universal KSU

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Kernel Source
        uses: actions/checkout@v4
        with:
          repository: hurt21-pixel/android_kernel_samsung_universal9810
          fetch-depth: 1

      - name: Setup Build Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y git-core gnupg flex bison build-essential zip curl zlib1g-dev libncurses-dev x11proto-core-dev libx11-dev libgl1-mesa-dev libxml2-utils xsltproc unzip fontconfig bc python3

      - name: Download Clang
        run: |
          mkdir -p $GITHUB_WORKSPACE/clang
          cd $GITHUB_WORKSPACE/clang
          # Pakai Neutron Clang terbaru
          bash <(curl -s "https://raw.githubusercontent.com/Neutron-Toolchains/antman/main/antman") -S

      - name: Setup KernelSU
        run: |
          # Langsung pasang KSU di root kernel
          curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -

      - name: Configure and Build
        run: |
          export PATH=$GITHUB_WORKSPACE/clang/bin:$PATH
          export ARCH=arm64
          export SUBARCH=arm64
          export CLANG_TRIPLE=aarch64-linux-gnu-
          export CROSS_COMPILE=aarch64-linux-android-
          export CROSS_COMPILE_ARM32=arm-linux-androideabi-
          
          # JURUS SAKTI 1: Hapus aturan -Werror agar warning tidak jadi error fatal
          find . -name Makefile -exec sed -i 's/-Werror//g' {} +
          find . -name Kbuild -exec sed -i 's/-Werror//g' {} +
          
          # Setting Config
          CONF=exynos9810_lineageos_defconfig
          
          # Inject Config KSU
          echo "CONFIG_KPROBES=y" >> arch/arm64/configs/$CONF
          echo "CONFIG_HAVE_KPROBES=y" >> arch/arm64/configs/$CONF
          echo "CONFIG_KPROBE_EVENTS=y" >> arch/arm64/configs/$CONF
          echo "CONFIG_KSU=y" >> arch/arm64/configs/$CONF
          
          # Matikan RKP/Knox agar tidak bootloop
          sed -i 's/CONFIG_RKP_CFP=y/CONFIG_RKP_CFP=n/g' arch/arm64/configs/$CONF
          sed -i 's/CONFIG_KNOX_KAP=y/CONFIG_KNOX_KAP=n/g' arch/arm64/configs/$CONF
          
          # JURUS SAKTI 2: Masak dengan LLVM=1 dan KCFLAGS="-w"
          # -w (huruf kecil) artinya: "Matikan semua peringatan, jangan bawel!"
          make O=out $CONF
          make -j$(nproc --all) O=out \
            LLVM=1 \
            LLVM_IAS=1 \
            CC=clang \
            CLANG_TRIPLE=$CLANG_TRIPLE \
            CROSS_COMPILE=$CROSS_COMPILE \
            CROSS_COMPILE_ARM32=$CROSS_COMPILE_ARM32 \
            KCFLAGS="-w"

      - name: Pack Kernel
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git AnyKernel3
          if [ -f out/arch/arm64/boot/Image.gz ]; then
            cp out/arch/arm64/boot/Image.gz AnyKernel3/
            cd AnyKernel3
            # Matikan cek device agar bisa di-flash ke S9/Note9 mana saja
            sed -i 's/do.devicecheck=1/do.devicecheck=0/g' anykernel.sh
            zip -r9 ../Kernel-S9-Universal-KSU.zip *
          else
            echo "Waduh Boss, Image.gz tidak ditemukan. Cek log compile di atas!" && exit 1
          fi

      - name: Upload Result
        uses: actions/upload-artifact@v4
        with:
          name: Kernel-S9-KSU-Final
          path: Kernel-S9-Universal-KSU.zip
          
